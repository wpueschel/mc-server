# {{ disclaimer }}
127.0.0.1       localhost
{% for host in base_setup_hosts -%}
{% if host.name == 'fritz.box' -%}
{{ '%-015s' | format(host.ip_address) }} {{ host.name }} {{ host.name }}
{% else -%}
{{ '%-015s' | format(host.ip_address) }} {{ host.name }}.{{ host.domain }} {{ host.name }}
{% endif -%}
{% endfor %}

# The following lines are desirable for IPv6 capable hosts
::1 localhost ip6-localhost ip6-loopback
ff02::1 ip6-allnodes
ff02::2 ip6-allrouters

